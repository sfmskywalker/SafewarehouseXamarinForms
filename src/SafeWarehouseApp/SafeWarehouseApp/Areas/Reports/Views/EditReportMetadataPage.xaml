<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="SafeWarehouseApp.Areas.Reports.Views.EditReportMetadataPage"
            Title="Meta">
    <StackLayout Spacing="15" Padding="15">
        <StackLayout>
            <Label Text="Opmerkingen" FontSize="Medium" />
            <Entry Text="{Binding Remarks, Mode=TwoWay}" FontSize="Medium" />
        </StackLayout>
        <StackLayout>
            <Label Text="Datum" FontSize="Medium" />
            <DatePicker Date="{Binding ReportDate, Mode=TwoWay}" FontSize="Medium" />
        </StackLayout>
        <StackLayout>
            <Label Text="Volgende keuring voor" FontSize="Medium" />
            <DatePicker Date="{Binding NextExaminationBefore, Mode=TwoWay}" FontSize="Medium" />
        </StackLayout>
    </StackLayout>
</ContentPage>